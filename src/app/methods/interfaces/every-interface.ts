/* Should be seperated it is only in one class for convenience */

export interface SelectedMethod {
  name: string;
  type: string;
  group: string;
}
export interface SuccessResponse {}

export interface ErrorResponse {}
