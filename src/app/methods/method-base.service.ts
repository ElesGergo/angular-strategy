import { Injectable } from '@angular/core';
import { BaseMethodFactory } from './interfaces/base-method.interface';

/* @Injectable()
export class BaseMethodService implements BaseMethodFactory {
  isApplicable() {
    return true;
  }

  continue() {
    return { success: 'yes' };
  }
}
 */
